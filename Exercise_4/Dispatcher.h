class Dispatcher
{
private:
    queue *S_dispatcher_queue;
    queue *N_dispatcher_queue;
    queue *W_dispatcher_queue;
    queue *ready;
    //pointer to vector of pointers to the producers queue
    std::queue<std::queue<std::string>*>*


public:
Dispatcher:Dispatcher(queue *S_dispatcher_queue, queue *N_dispatcher_queue, queue *W_dispatcher_queue, std::queue<std::queue<std::string>*>*)
    for (int i=0; i<numOfProducers; ++i) {
        if (queue[])
    }
};

